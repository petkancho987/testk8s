apiVersion: v1
kind: Service
metadata:
  name: github-demo-service
  namespace: github-demo

spec:
  selector:
    app: github-demo

  ports:
    - port: 80
      targetPort: 3000

  type: LoadBalancer
